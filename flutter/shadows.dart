import 'package:flutter/material.dart';

class AppShadows {
  static const boxShadowSpreadButtonPrimaryHover = BoxShadow(
    color: Color(0xfff79009),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 1,
  );

  static const boxShadowSpreadButtonPrimaryPressed = BoxShadow(
    color: Color(0xff70707b),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const boxShadowSpreadButtonPrimaryFocused = BoxShadow(
    color: Color(0xfff79009),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const boxShadowSpreadButtonSecondaryHover = BoxShadow(
    color: Color(0xff70707b),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 1,
  );

  static const boxShadowSpreadButtonSecondaryPressed = BoxShadow(
    color: Color(0xff70707b),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const boxShadowSpreadButtonSecondaryFocused = BoxShadow(
    color: Color(0xff70707b),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 1,
  );

  static const boxShadowSpreadButtonCriticalHover = BoxShadow(
    color: Color(0xfff04438),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 1,
  );

  static const boxShadowSpreadButtonCriticalPressed = BoxShadow(
    color: Color(0xfff04438),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  static const boxShadowSpreadButtonCriticalFocused = BoxShadow(
    color: Color(0xfff04438),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 1,
  );

  static const boxShadowElevation100 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const boxShadowElevation200 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const boxShadowElevation300 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: -2,
  );

  static const boxShadowElevation400 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 4),
    blurRadius: 6,
    spreadRadius: -2,
  );

  static const boxShadowElevation500 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 8),
    blurRadius: 8,
    spreadRadius: -4,
  );

  static const boxShadowElevation600 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 24),
    blurRadius: 48,
    spreadRadius: -12,
  );

  static const boxShadowElevation700 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 32),
    blurRadius: 64,
    spreadRadius: -12,
  );

  static const boxShadowElevationFocus100 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const boxShadowElevationFocus200 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 1),
    blurRadius: 2,
    spreadRadius: 0,
  );

  static const boxShadowElevationFocus300 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 2),
    blurRadius: 4,
    spreadRadius: -2,
  );

  static const boxShadowElevationFocus400 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 4),
    blurRadius: 6,
    spreadRadius: -2,
  );

  static const boxShadowElevationFocus500 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 8),
    blurRadius: 8,
    spreadRadius: -4,
  );

  static const boxShadowElevationFocus600 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 24),
    blurRadius: 48,
    spreadRadius: -12,
  );

  static const boxShadowElevationFocus700 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 32),
    blurRadius: 64,
    spreadRadius: -12,
  );

  static const boxShadowFocusPrimary = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(0, 0),
    blurRadius: 0,
    spreadRadius: 2,
  );

  AppShadows._();
}
